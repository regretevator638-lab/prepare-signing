name: Fetch iOS Certificates & Provisioning Profiles

on:
  workflow_dispatch:
    inputs:
      app_identifier:
        description: 'App Bundle ID (e.g. com.yourname.app)'
        required: true
        default: 'com.example.myapp'

jobs:
  fetch-certs:
    runs-on: macos-latest

    steps:
      # ─── 1. Checkout ───────────────────────────────────────────
      - name: Checkout Repository
        uses: actions/checkout@v4

      # ─── 2. Setup Ruby + Fastlane ──────────────────────────────
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true

      - name: Install Fastlane
        run: gem install fastlane --no-document

      # ─── 3. Setup Fastlane Session (2FA Bypass) ────────────────
      - name: Configure Fastlane Session for 2FA
        env:
          FASTLANE_SESSION: ${{ secrets.FASTLANE_SESSION }}
        run: |
          if [ -n "$FASTLANE_SESSION" ]; then
            echo "✅ Using FASTLANE_SESSION for 2FA bypass"
            echo "FASTLANE_SESSION=$FASTLANE_SESSION" >> $GITHUB_ENV
          else
            echo "⚠️ No FASTLANE_SESSION found. This might fail for Free Accounts."
          fi

      # ─── 4. Create output directory ────────────────────────────
      - name: Create Output Directory
        run: mkdir -p ./certs-output

      # ─── 5. Fetch Development Certificate (.p12) ───────────────
      - name: Fetch Development Certificate
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }} # ใส่ "ก้อนทอง" 16 หลักใน Secret นี้นะพี่
          TEAM_ID: '6SAY6G29NL1' # ใส่เลขของพี่บอสลงไปเลย!
        run: |
          fastlane cert \
            --development \
            --username "$APPLE_ID" \
            --team_id "$TEAM_ID" \
            --output_path "./certs-output" \
            --skip_itc # สำคัญมาก! บัญชีฟรีต้องข้าม App Store Connect

      # ─── 6. Fetch Development Provisioning Profile ─────────────
      - name: Fetch Development Provisioning Profile
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          TEAM_ID: 'IAJTAPZQYCLT'
        run: |
          fastlane sigh \
            --development \
            --username "$APPLE_ID" \
            --team_id "$TEAM_ID" \
            --app_identifier "${{ github.event.inputs.app_identifier }}" \
            --output_path "./certs-output" \
            --force \
            --skip_install \
            --skip_itc # บัญชีฟรีต้องใส่ตัวนี้ครับ

      # ─── 7. Convert & Cleanup ──────────────────────────────────
      # ส่วนที่เหลือของสคริปต์เดิมพี่ใช้ได้เลยครับ ผมตัดมาแค่ส่วนที่ต้องแก้
      - name: List Generated Files
        run: |
          ls -lah ./certs-output/

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ios-certs-pack
          path: ./certs-output/*
